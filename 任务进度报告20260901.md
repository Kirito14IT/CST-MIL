# 任务进度报告

> 报告日期：2026 年 9 月 1 日  
> 研究领域：网络欺凌、辱骂、言语暴力  
> 研究对象：一条微博原帖及其评论构成的真实多人会话

## 一、任务内容

本阶段完成了三项工作：一是下载并核验 LDA、BTM、GSDMM、BERTopic 的固定版本；NMF 未发现 Lee–Seung 原作者维护的官方仓库。五种方法原生用于主题发现，未经会话聚合和分类头适配，不能直接完成 SCCD 会话级风险判别，本阶段也尚未进行基线性能对比。二是核验 LCCC-base：它适合普通中文对话处理，但缺少危险标签、稳定说话人和完整回复结构，且过滤了大量敏感内容，因而与本任务不匹配。三是将研究范围固定为“网络欺凌、辱骂、言语暴力”，采用 SCCD 开展会话级 `CB / Non-CB` 二分类，并在 [`cst_mil/`](cst_mil/) 实现不依赖大语言模型、预训练语言模型或 GPU 的 CST-MIL。

SCCD原始数据含 677 个会话和 38,999 条评论；去除完全重复、冲突 ID 和孤立评论后保留 38,872 条。评论中位数为 13 字，55.21% 集中在 5–20 字，69.73% 不超过 20 字，说明其符合短文本高稀疏性；同时保留匿名用户、时间顺序和 7,868 条回复指针，比 LCCC-base 更接近多人会话研究需求。

### CST-MIL 方法与固定实验

![CST-MIL 方法流程](cst_mil/artifacts/run_001/analysis/figures/figure-03-method-pipeline.png)

CST-MIL 首先利用字符 2–5 gram 和线性分类器估计评论风险；训练时只使用会话标签，通过 3 轮多实例学习在正会话中选择得分最高的 20% 评论；随后以 8 主题 MiniBatchNMF 提取隐主题及主题变化，并融合评论风险、回复关系和会话统计，由 Logistic Regression 输出最终风险概率及 Top-3 高风险上下文候选。评论级标签仅用于测试阶段的证据定位评价，不作为训练特征。

固定随机种子 42，从 SCCD 抽取 150 个 CB 和 150 个 Non-CB，会话按 `post_id` 划分为训练集 180、验证集 60、测试集 60，同一会话不跨集合。正式模型只训练一次，验证集确定阈值 0.5632。

| 测试指标 | 结果 | 条件性 95% bootstrap 区间 |
|---|---:|---:|
| Accuracy / Macro-F1 | 81.67% / 81.62% | Macro-F1：71.47%–91.61% |
| CB Precision / Recall / F1 | 85.19% / 76.67% / 80.70% | Recall：60.00%–90.00% |
| ROC-AUC / 平均精确率 AP | 90.11% / 90.43% | AP：83.13%–96.90% |
| 混淆矩阵 | TN=26，FP=4，FN=7，TP=23 | — |
| CPU 效率 | 12.05 ms/会话，模型 3.58 MiB | 推理峰值内存约 295 MiB |

会话分类已表现出初步可行性，但证据定位仍弱：评论级 AP 为 29.25%，含危险评论会话的 Top-3 命中率为 56.82%。由于实例由“父评论＋当前评论”构成，Top-3 只能解释为高风险上下文候选，不能视为严格的单句归因。

## 二、作出的决定和收获

1. **研究对象已经固定。** 采用 SCCD，以“原帖＋按时间排序的全部评论”为一个样本。它是真实微博树状会话，不等同于微信或 QQ 群聊；LCCC-base 并非质量差，而是与危险会话评测目标不匹配。
2. **三个任务难点需要分别判断。** 评论中位数仅 13 字，直接支持高稀疏性；语料保留口语、隐式攻击和讽刺，但 URL、提及和表情等已被清洗，只能部分支持强噪声性；静态 SCCD 不能验证快速演化性，字符哈希和 SGD 也只是为后续增量更新提供技术基础。
3. **会话判别与证据定位不能混为一谈。** 一次固定小样本实验取得 81.62% macro-F1，说明低成本路线能够运行；但评论证据仍不够可靠，且主题发现不等于风险判别，因此既不能替代人工核验，也不能宣称已经稳定优于五种基线。

## 三、存在的问题及后续改进措施

| 当前问题 | 下一阶段措施 |
|---|---|
| SCCD 只覆盖微博网络欺凌，且仓库缺少清晰的独立 LICENSE | 限定结论范围，不重新分发原始数据；公开派生数据前确认授权，并补充更接近即时群聊的数据。 |
| 本轮仅 300 个会话、一个划分、一次训练 | 扩展到完整 SCCD，至少运行 5 个随机种子，报告均值、标准差和置信区间。 |
| 仅保证 `post_id` 不跨集合；train/test 仍重叠 7 个原帖作者、93 个评论用户及 35 种相同文本 | 模型虽未使用用户 ID，仍需增加用户、事件和近重复文本分组划分，重新检查风格影响与泛化性能。 |
| 评论证据定位明显弱于会话分类，仍有 7 个危险会话漏检 | 优先提高 CB 召回率，区分父评论与当前评论贡献，并尝试层次化 MIL、实例校准和评论级辅助损失。 |
| 五种基线尚未在 SCCD 上公平比较 | 统一清洗、划分和 Logistic Regression 分类头，将 LDA、BTM、GSDMM、NMF、BERTopic 的输出转为会话主题向量；分别比较单条评论、固定窗口和完整会话。 |
| 尚未验证快速演化能力 | 增加时间切分、漂移监测和 `partial_fit` 增量更新实验，完成后再评价暗语及话题演化适应能力。 |

下一阶段将以相同数据协议比较 CST-MIL 与五种适配基线，统一报告 macro-F1、CB Recall、AP、主题质量、运行时间和峰值内存，并开展字符通道、隐主题、回复结构和 MIL 聚合的消融实验。

> **阶段结论：**CST-MIL 已在一次固定的 SCCD 小规模实验中证明轻量级会话风险检测流程可运行，取得 81.62% macro-F1 和约 12.05 ms/会话的本机 CPU 推理速度。现有结果属于可行性证据，不代表已证明多次训练稳定性、快速演化能力或相对五种基线的优越性。

资料与完整产物：[SCCD 官方仓库](https://github.com/STAIR-BUPT/SCCD)、[`baseline/README.md`](baseline/README.md)、[`cst_mil/artifacts/run_001/`](cst_mil/artifacts/run_001/)。
